# 垒球训练辅助系统 · 开发原则

> 目标：以可扩展的大型网站标准约束当前 MVP，避免「能演示但不能演进」。
> 本文件优先于临时对话指令；与 `.cursorrules` 冲突时，以本文件的工程条款 + `.cursorrules` 的产品纪律共同为准。

---

## 0. 产品定位（来自业务纪律）

1. **录入优先，不做分析噪音**：测试现场是高频录入终端，不做实时复杂计算看板。
2. **界面克制**：黑 / 白 / 灰高对比；拒绝花哨动画与装饰性 UI。
3. **先本地，后云端**：当前以浏览器本地状态跑通闭环；云端同步必须显式、可回滚。
4. **内容未齐不纠结文案**：未定稿文案用占位即可，但**数据结构与契约必须一次想清楚**。

---

## 1. 架构分层（大型站点目标）

```
UI（app/**/page.tsx）
  → domain hooks / services（业务规则）
    → repositories（读写抽象：local 或 API）
      → persistence（localStorage / DB / OSS）
```

### 必须遵守

- **页面文件禁止继续膨胀**：单文件超过 ~400 行应拆分组件与 hooks。
- **禁止在多个页面复制同一数据契约**：Player / Hit / GameArchive / CurrentUser 只定义一处（`lib/` 或未来 `packages/shared`）。
- **UI 不直接散落 `localStorage.getItem/setItem`**：统一走 repository；页面只操作 state / hook。
- **写入端与读取端共用同一类型与版本化 schema**，禁止「这一页写 `hits`、那一页读 `data`」且无兼容层。

---

## 2. 数据契约与版本兼容（强制）

### Schema 版本

- 所有持久化对象建议带 `schemaVersion: number`（或等价字段）。
- **改字段名 / 改结构 = 发新版本**，并提供 `migrate(old) → new`。
- 读取时一律安全提取，例如：
  - `const hits = game.hits ?? game.data ?? []`
  - 禁止对可能为 `undefined` 的数组直接 `.some/.filter/.map`

### localStorage 键名清单（须集中管理）

代码唯一来源：`lib/storageKeys.ts`。

| Key | 用途 | 备注 |
|-----|------|------|
| `softball_players` | 球员名册 | 与登录/排阵共用 |
| `softball_currentUser` | 当前登录身份 | 无 TTL，退出只清此键 |
| `softball_session_draft` | 当前场次草稿 | 含 hits / speedRecords / assignments / testItems；带 `schemaVersion` |
| `softball_hits` | （已废弃）旧版当场打点 | 读取时迁入 `session_draft` 后清除 |
| `softball_games_history` | 历史归档 | 新旧格式必须兼容；条目带 `schemaVersion` |
| `softball_readiness_history` | 每日综合状态评估时序 | 按 playerId+date upsert |
| `softball_injury_log` | 伤病处方归档 | prehab 写入；个人档案读取；带 `schemaVersion` |

新增 key 必须写入本表与 `lib/storageKeys.ts`；禁止魔法字符串散落。

### 禁止

- 静默覆盖用户数据（未挂载完成就 `setItem` 空数组）。
- 无确认的清空 / 归档。
- 多标签页互相覆盖且无合并策略（大型站点需 `storage` 事件或后端权威源）。

---

## 3. Next.js / React 工程纪律

1. **Hydration 安全**
   - 禁止在 render 阶段读 `window` / `localStorage` / `Date.now()` 驱动 UI 分支。
   - 首屏服务端与客户端第一次渲染必须一致；浏览器数据在 `useEffect` / `useSyncExternalStore` 之后注入。
2. **Server / Client 边界**
   - 使用 hooks、事件、浏览器 API 的组件必须 `'use client'`。
   - 不要为了省事把整个树都标成 client。
3. **环境变量**
   - 密钥永不进 `NEXT_PUBLIC_*`。
   - `NEXT_PUBLIC_*` 在 **build 时内联**；改值必须重建部署。
   - 生产环境变量在托管平台配置，不依赖本机 `.env.local`。
4. **错误与空态**
   - 关键路径有明确空态 / 错误态，禁止白屏。
   - JSON.parse 必须 try/catch；解析失败有兜底，不拖垮整页。

---

## 4. 身份与安全（上线前必达）

当前身份模型是「本地选择球员 = 登录」，**不可用于真实多用户生产**。

上线路径：

1. 真实鉴权（Session / JWT / OAuth），服务端校验。
2. 所有写操作按 `userId/teamId` 隔离；禁止只靠前端藏按钮。
3. 输入校验与输出转义；表单防刷（rate limit）。
4. 不在客户端存明文敏感信息。
5. 生产关闭或保护调试 `console.log` 中的业务载荷。

---

## 5. AI / Cursor 协作纪律（防「越修越坏」）

1. **一次一事**：一条指令只改一个模块；改完预览再下一条。
2. **大改前存档**：git commit 或明确 checkpoint；禁止靠聊天「undo」。
3. **禁止「修一下」空指令**：必须写清文件、行为、验收标准。
4. **改 schema 必须同步读写双方 + 迁移/fallback**，并在 PR/说明里写版本影响。
5. **不中途换栈**（例如突然引入另一套状态库）除非有迁移计划。
6. **真内容未定时，仍要定字段名与类型**；占位文案可以假，契约不能假。

---

## 6. 可维护性（大型站点标准）

| 项 | 要求 |
|----|------|
| 组件 | 可复用交互抽到 `components/`；页面只编排 |
| 命名 | 中英双语 UI 标签集中字典化，避免魔法字符串 |
| 测试 | 核心解析/迁移/归档函数至少有单元测试 |
| 可观测 | 生产错误上报（Sentry 等）；关键业务埋点 |
| 性能 | 图片走优化管线；列表虚拟化；避免无意义整页重渲染 |
| 文档 | `开发原则.md` + 数据契约说明；新模块补最短 README |
| 依赖 | 锁版本；定期审计；不堆无用插件 |

---

## 7. 部署与运行时（Vercel + 阿里云）

### 推荐职责切分

| 层 | 建议承载 | 说明 |
|----|----------|------|
| 前端 / BFF | **Vercel**（Next.js） | SSR/ISR、预览环境、边缘网络 |
| 业务 API / 数据库 | **阿里云**（ECS/FC + RDS/PostgreSQL 或阿里云版 Supabase 替代） | 数据主权、国内延迟、备份 |
| 文件 / 导出 | **阿里云 OSS** | JSON 归档、图片、备份 |
| 域名 / CDN | 阿里云 DNS +（可选）国内 CDN | 若主用户在国内，需单独评估 Vercel 可达性 |

### 原则

1. **浏览器直连的权威数据源只有一个**（最终应是云端 API，不是 localStorage）。
2. localStorage 降级为「离线草稿 / 缓存」，冲突以服务端为准。
3. 跨域：API 明确 CORS；Cookie 方案注意 `SameSite` / 域名。
4. 中国大陆访问：若 Vercel 不稳定，备选 **阿里云 SAE/ECS 自托管 Next** 或 **前端静态导出 + 阿里云 Nginx**，API 仍在阿里云。
5. 密钥与连接串只放在服务端环境变量；CI/CD 分环境（preview / production）。

### 明确不可行的用法

- 仅把静态站丢 Vercel，却把「含密钥的写库逻辑」写在客户端。
- Vercel 与阿里云各写一套互不同步的用户数据。
- 无备份策略的「只存在浏览器里的正式成绩」。

---

## 8. 功能演进顺序（建议）

1. **契约冻结**：统一 `GameArchive` / Player / User，带 version + migrate。  
2. **拆分巨石页**：`app/page.tsx` 拆 sidebar / tee-ball / speed-test / archive。  
3. **引入后端**：成绩与名册上云；本地作草稿。  
4. **真鉴权 + 权限**（教练 / 队员）。  
5. **观测、备份、CI、预览环境**。  
6. 再扩更多测试项与分析页（分析仍非录入主路径）。

---

## 9. Code Review 检查清单（每次合并前）

- [ ] 是否新增/变更了持久化 schema？有无 fallback 或 migrate？
- [ ] 是否在 render 期触达浏览器 API？
- [ ] 是否在多个文件复制了同一业务规则？
- [ ] 空数据 / 坏 JSON / 未登录路径是否安全？
- [ ] 归档/清空是否有确认？归档后盘面是否清空完整？
- [ ] 是否引入了与「克制 UI / 录入优先」相冲突的装饰或实时分析？
- [ ] 环境变量是否分清 public / secret？
- [ ] 大文件是否继续膨胀？该拆未拆？

---

## 10. 变更记录

| 日期 | 说明 |
|------|------|
| 2026-07-24 | 初版：基于全库审计与「大型站点」目标写入；含 Vercel+阿里云职责切分 |
| 2026-07-24 | 契约起步：集中 storageKeys；session_draft 取代散落 hits；GameArchive 带 version + migrate |
| 2026-07-24 | 临床字典与鉴权收口：cycle→assessment；injury_log；useRequireAuth；Navbar 含 prehab |
| 2026-07-24 | 拆分测试日巨石页：useTestDaySession + AssignmentSidebar / TeeBall / Speed 面板 |
